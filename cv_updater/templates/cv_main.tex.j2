%%%%%%%%%%%%%%%
% LaTeX CV generated by cv-updater
\documentclass[a4paper,skipsamekey,11pt,english]{curve}

\PassOptionsToPackage{style=ieee,sorting=ydnt,uniquename=init,defernumbers=true}{biblatex}

% Pre-declare no-math option for fontspec to prevent option clash with cochineal
\PassOptionsToPackage{no-math}{fontspec}

\usepackage{settings}

\DefineBibliographyStrings{english}{url={\textsc{url}}}

\addbibresource{own-bib.bib}

<% if mynames %>\mynames{<< mynames >>}
<% endif %>

\ifxetexorluatex
  \usepackage[p,osf,swashQ]{cochineal}
  \usepackage[medium,bold]{cabin}
  \usepackage[varqu,varl,scale=0.9]{zi4}
\else
  \usepackage[T1]{fontenc}
  \usepackage[p,osf,swashQ]{cochineal}
  \usepackage{cabin}
  \usepackage[varqu,varl,scale=0.9]{zi4}
\fi

<% if prefix_marker %>% Custom prefix marker (overrides \faBookmark default)
\prefixmarker{<< prefix_marker >>}
<% endif %>

\includecomment{fullonly}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\leftheader{%
  {\LARGE\bfseries\sffamily << personal.name >>}

  \makefield{\faEnvelope[regular]}{\href{mailto:<< personal.email >>}{\texttt{<< personal.email >>}}}
<% if personal.linkedin %>
  \makefield{\faLinkedin}
  {\href{<< personal.linkedin >>}{\texttt{<< personal.linkedin_label or personal.linkedin >>}}}
<% endif %>

<% if personal.github %>
  \makefield{\faGlobe}{\url{<< personal.github >>}}
<% endif %>
}

\rightheader{~}
<% if not personal.skip_photo %>
\begin{fullonly}
\photo[r]{<< personal.photo or 'photo' >>}
\photoscale{0.13}
\end{fullonly}
<% endif %>

\title{Curriculum Vitae}

\begin{document}
\makeheaders[c]

<% for section_key in section_order %><% if section_key not in skipped_sections %>\makerubric{<< section_key >>}
<% endif %><% endfor %>
\end{document}
